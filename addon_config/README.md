# configdir for each kubernetes cluster addons
## each addons will provide two ways:
    
    1.helm 
    2.manual

There will provides custom values.yaml in helm dir, whole custom kubernetes api yaml in manual dir.